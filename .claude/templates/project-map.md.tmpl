# Project map — <PROJECT NAME>

<!-- The CURRENT architecture snapshot. Overwritten as the project changes
     (by bootstrap initially, by maintainer over time). This is the "what is
     it now" file — for the "why is it like this" history, see docs/build-log.md.

     Budget: 60 lines. Paths and one-liners, never pasted code.
     This file exists to stop the planner crawling the repo on every order, so
     it is read constantly and its length is paid constantly. A map that grows
     into a mirror of the codebase costs more than the crawl it replaced. When
     it stops fitting, cut detail — the code is still there to read. -->

## Overview
<What the system is, its primary surfaces, and the tech stack.>

## Structure
<Directory-by-directory: what each area is responsible for.>

## Key entities / modules
<Core data models, services, or components and how they relate.>

## The team
<The agents active on this project — core team plus any specialists — and
 a one-line note on what each is for. Kept in sync with .claude/agents/.>

## Open unknowns / blockers
<Anything derived-but-uncertain, or awaiting a decision. Keep explicit.>
